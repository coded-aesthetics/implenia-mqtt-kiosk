node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
/app-next/
*.tar.gz
# Vosk speech model (downloaded via scripts/download-vosk-model.sh)
ui/public/vosk-model-de.tar.gz

# Raw MQTT field captures — reference data, too large for the repo.
# Kept alongside it locally; see assets/reference/README.md.
assets/*.txt
