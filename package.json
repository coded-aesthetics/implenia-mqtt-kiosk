{
  "name": "implenia-kiosk",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "server",
    "ui"
  ],
  "scripts": {
    "dev": "concurrently -n server,ui -c blue,green \"npm run dev -w server\" \"npm run dev -w ui\"",
    "build": "npm run build -w server && npm run build -w ui",
    "start": "node server/dist/index.js",
    "lint": "npm run lint -w server && npm run lint -w ui"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
